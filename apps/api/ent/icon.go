// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"project-management-demo-backend/ent/icon"
	"project-management-demo-backend/ent/schema/ulid"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Icon is the model entity for the Icon schema.
type Icon struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Icon holds the value of the "icon" field.
	Icon string `json:"icon,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IconQuery when eager-loading is set.
	Edges        IconEdges `json:"edges"`
	selectValues sql.SelectValues
}

// IconEdges holds the relations/edges for other nodes in the graph.
type IconEdges struct {
	// ProjectIcons holds the value of the projectIcons edge.
	ProjectIcons []*ProjectIcon `json:"projectIcons,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedProjectIcons map[string][]*ProjectIcon
}

// ProjectIconsOrErr returns the ProjectIcons value or an error if the edge
// was not loaded in eager-loading.
func (e IconEdges) ProjectIconsOrErr() ([]*ProjectIcon, error) {
	if e.loadedTypes[0] {
		return e.ProjectIcons, nil
	}
	return nil, &NotLoadedError{edge: "projectIcons"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Icon) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case icon.FieldName, icon.FieldIcon:
			values[i] = new(sql.NullString)
		case icon.FieldCreatedAt, icon.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case icon.FieldID:
			values[i] = new(ulid.ID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Icon fields.
func (_m *Icon) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case icon.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case icon.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case icon.FieldIcon:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field icon", values[i])
			} else if value.Valid {
				_m.Icon = value.String
			}
		case icon.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case icon.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Icon.
// This includes values selected through modifiers, order, etc.
func (_m *Icon) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProjectIcons queries the "projectIcons" edge of the Icon entity.
func (_m *Icon) QueryProjectIcons() *ProjectIconQuery {
	return NewIconClient(_m.config).QueryProjectIcons(_m)
}

// Update returns a builder for updating this Icon.
// Note that you need to call Icon.Unwrap() before calling this method if this Icon
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Icon) Update() *IconUpdateOne {
	return NewIconClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Icon entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Icon) Unwrap() *Icon {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Icon is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Icon) String() string {
	var builder strings.Builder
	builder.WriteString("Icon(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("icon=")
	builder.WriteString(_m.Icon)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedProjectIcons returns the ProjectIcons named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *Icon) NamedProjectIcons(name string) ([]*ProjectIcon, error) {
	if _m.Edges.namedProjectIcons == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedProjectIcons[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *Icon) appendNamedProjectIcons(name string, edges ...*ProjectIcon) {
	if _m.Edges.namedProjectIcons == nil {
		_m.Edges.namedProjectIcons = make(map[string][]*ProjectIcon)
	}
	if len(edges) == 0 {
		_m.Edges.namedProjectIcons[name] = []*ProjectIcon{}
	} else {
		_m.Edges.namedProjectIcons[name] = append(_m.Edges.namedProjectIcons[name], edges...)
	}
}

// Icons is a parsable slice of Icon.
type Icons []*Icon
