// Code generated by ent, DO NOT EDIT.

package ent

import (
	"asana-clone-app/ent/predicate"
	"asana-clone-app/ent/schema/ulid"
	"asana-clone-app/ent/task"
	"asana-clone-app/ent/taskfeed"
	"asana-clone-app/ent/taskfeedlike"
	"asana-clone-app/ent/taskfile"
	"asana-clone-app/ent/teammate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TaskFeedQuery is the builder for querying TaskFeed entities.
type TaskFeedQuery struct {
	config
	ctx                    *QueryContext
	order                  []taskfeed.OrderOption
	inters                 []Interceptor
	predicates             []predicate.TaskFeed
	withTask               *TaskQuery
	withTeammate           *TeammateQuery
	withTaskFeedLikes      *TaskFeedLikeQuery
	withTaskFiles          *TaskFileQuery
	modifiers              []func(*sql.Selector)
	loadTotal              []func(context.Context, []*TaskFeed) error
	withNamedTaskFeedLikes map[string]*TaskFeedLikeQuery
	withNamedTaskFiles     map[string]*TaskFileQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TaskFeedQuery builder.
func (_q *TaskFeedQuery) Where(ps ...predicate.TaskFeed) *TaskFeedQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TaskFeedQuery) Limit(limit int) *TaskFeedQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TaskFeedQuery) Offset(offset int) *TaskFeedQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TaskFeedQuery) Unique(unique bool) *TaskFeedQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TaskFeedQuery) Order(o ...taskfeed.OrderOption) *TaskFeedQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTask chains the current query on the "task" edge.
func (_q *TaskFeedQuery) QueryTask() *TaskQuery {
	query := (&TaskClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(taskfeed.Table, taskfeed.FieldID, selector),
			sqlgraph.To(task.Table, task.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, taskfeed.TaskTable, taskfeed.TaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeammate chains the current query on the "teammate" edge.
func (_q *TaskFeedQuery) QueryTeammate() *TeammateQuery {
	query := (&TeammateClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(taskfeed.Table, taskfeed.FieldID, selector),
			sqlgraph.To(teammate.Table, teammate.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, taskfeed.TeammateTable, taskfeed.TeammateColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTaskFeedLikes chains the current query on the "taskFeedLikes" edge.
func (_q *TaskFeedQuery) QueryTaskFeedLikes() *TaskFeedLikeQuery {
	query := (&TaskFeedLikeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(taskfeed.Table, taskfeed.FieldID, selector),
			sqlgraph.To(taskfeedlike.Table, taskfeedlike.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, taskfeed.TaskFeedLikesTable, taskfeed.TaskFeedLikesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTaskFiles chains the current query on the "taskFiles" edge.
func (_q *TaskFeedQuery) QueryTaskFiles() *TaskFileQuery {
	query := (&TaskFileClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(taskfeed.Table, taskfeed.FieldID, selector),
			sqlgraph.To(taskfile.Table, taskfile.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, taskfeed.TaskFilesTable, taskfeed.TaskFilesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TaskFeed entity from the query.
// Returns a *NotFoundError when no TaskFeed was found.
func (_q *TaskFeedQuery) First(ctx context.Context) (*TaskFeed, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{taskfeed.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TaskFeedQuery) FirstX(ctx context.Context) *TaskFeed {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TaskFeed ID from the query.
// Returns a *NotFoundError when no TaskFeed ID was found.
func (_q *TaskFeedQuery) FirstID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{taskfeed.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TaskFeedQuery) FirstIDX(ctx context.Context) ulid.ID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TaskFeed entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TaskFeed entity is found.
// Returns a *NotFoundError when no TaskFeed entities are found.
func (_q *TaskFeedQuery) Only(ctx context.Context) (*TaskFeed, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{taskfeed.Label}
	default:
		return nil, &NotSingularError{taskfeed.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TaskFeedQuery) OnlyX(ctx context.Context) *TaskFeed {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TaskFeed ID in the query.
// Returns a *NotSingularError when more than one TaskFeed ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TaskFeedQuery) OnlyID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{taskfeed.Label}
	default:
		err = &NotSingularError{taskfeed.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TaskFeedQuery) OnlyIDX(ctx context.Context) ulid.ID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TaskFeeds.
func (_q *TaskFeedQuery) All(ctx context.Context) ([]*TaskFeed, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TaskFeed, *TaskFeedQuery]()
	return withInterceptors[[]*TaskFeed](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TaskFeedQuery) AllX(ctx context.Context) []*TaskFeed {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TaskFeed IDs.
func (_q *TaskFeedQuery) IDs(ctx context.Context) (ids []ulid.ID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(taskfeed.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TaskFeedQuery) IDsX(ctx context.Context) []ulid.ID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TaskFeedQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TaskFeedQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TaskFeedQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TaskFeedQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TaskFeedQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TaskFeedQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TaskFeedQuery) Clone() *TaskFeedQuery {
	if _q == nil {
		return nil
	}
	return &TaskFeedQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]taskfeed.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.TaskFeed{}, _q.predicates...),
		withTask:          _q.withTask.Clone(),
		withTeammate:      _q.withTeammate.Clone(),
		withTaskFeedLikes: _q.withTaskFeedLikes.Clone(),
		withTaskFiles:     _q.withTaskFiles.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithTask tells the query-builder to eager-load the nodes that are connected to
// the "task" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithTask(opts ...func(*TaskQuery)) *TaskFeedQuery {
	query := (&TaskClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTask = query
	return _q
}

// WithTeammate tells the query-builder to eager-load the nodes that are connected to
// the "teammate" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithTeammate(opts ...func(*TeammateQuery)) *TaskFeedQuery {
	query := (&TeammateClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeammate = query
	return _q
}

// WithTaskFeedLikes tells the query-builder to eager-load the nodes that are connected to
// the "taskFeedLikes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithTaskFeedLikes(opts ...func(*TaskFeedLikeQuery)) *TaskFeedQuery {
	query := (&TaskFeedLikeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTaskFeedLikes = query
	return _q
}

// WithTaskFiles tells the query-builder to eager-load the nodes that are connected to
// the "taskFiles" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithTaskFiles(opts ...func(*TaskFileQuery)) *TaskFeedQuery {
	query := (&TaskFileClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTaskFiles = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TaskID ulid.ID `json:"task_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TaskFeed.Query().
//		GroupBy(taskfeed.FieldTaskID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TaskFeedQuery) GroupBy(field string, fields ...string) *TaskFeedGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TaskFeedGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = taskfeed.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TaskID ulid.ID `json:"task_id,omitempty"`
//	}
//
//	client.TaskFeed.Query().
//		Select(taskfeed.FieldTaskID).
//		Scan(ctx, &v)
func (_q *TaskFeedQuery) Select(fields ...string) *TaskFeedSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TaskFeedSelect{TaskFeedQuery: _q}
	sbuild.label = taskfeed.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TaskFeedSelect configured with the given aggregations.
func (_q *TaskFeedQuery) Aggregate(fns ...AggregateFunc) *TaskFeedSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TaskFeedQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !taskfeed.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TaskFeedQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TaskFeed, error) {
	var (
		nodes       = []*TaskFeed{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withTask != nil,
			_q.withTeammate != nil,
			_q.withTaskFeedLikes != nil,
			_q.withTaskFiles != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TaskFeed).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TaskFeed{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTask; query != nil {
		if err := _q.loadTask(ctx, query, nodes, nil,
			func(n *TaskFeed, e *Task) { n.Edges.Task = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeammate; query != nil {
		if err := _q.loadTeammate(ctx, query, nodes, nil,
			func(n *TaskFeed, e *Teammate) { n.Edges.Teammate = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTaskFeedLikes; query != nil {
		if err := _q.loadTaskFeedLikes(ctx, query, nodes,
			func(n *TaskFeed) { n.Edges.TaskFeedLikes = []*TaskFeedLike{} },
			func(n *TaskFeed, e *TaskFeedLike) { n.Edges.TaskFeedLikes = append(n.Edges.TaskFeedLikes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTaskFiles; query != nil {
		if err := _q.loadTaskFiles(ctx, query, nodes,
			func(n *TaskFeed) { n.Edges.TaskFiles = []*TaskFile{} },
			func(n *TaskFeed, e *TaskFile) { n.Edges.TaskFiles = append(n.Edges.TaskFiles, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedTaskFeedLikes {
		if err := _q.loadTaskFeedLikes(ctx, query, nodes,
			func(n *TaskFeed) { n.appendNamedTaskFeedLikes(name) },
			func(n *TaskFeed, e *TaskFeedLike) { n.appendNamedTaskFeedLikes(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedTaskFiles {
		if err := _q.loadTaskFiles(ctx, query, nodes,
			func(n *TaskFeed) { n.appendNamedTaskFiles(name) },
			func(n *TaskFeed, e *TaskFile) { n.appendNamedTaskFiles(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TaskFeedQuery) loadTask(ctx context.Context, query *TaskQuery, nodes []*TaskFeed, init func(*TaskFeed), assign func(*TaskFeed, *Task)) error {
	ids := make([]ulid.ID, 0, len(nodes))
	nodeids := make(map[ulid.ID][]*TaskFeed)
	for i := range nodes {
		fk := nodes[i].TaskID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(task.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "task_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TaskFeedQuery) loadTeammate(ctx context.Context, query *TeammateQuery, nodes []*TaskFeed, init func(*TaskFeed), assign func(*TaskFeed, *Teammate)) error {
	ids := make([]ulid.ID, 0, len(nodes))
	nodeids := make(map[ulid.ID][]*TaskFeed)
	for i := range nodes {
		fk := nodes[i].TeammateID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(teammate.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "teammate_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TaskFeedQuery) loadTaskFeedLikes(ctx context.Context, query *TaskFeedLikeQuery, nodes []*TaskFeed, init func(*TaskFeed), assign func(*TaskFeed, *TaskFeedLike)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[ulid.ID]*TaskFeed)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(taskfeedlike.FieldTaskFeedID)
	}
	query.Where(predicate.TaskFeedLike(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(taskfeed.TaskFeedLikesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TaskFeedID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "task_feed_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TaskFeedQuery) loadTaskFiles(ctx context.Context, query *TaskFileQuery, nodes []*TaskFeed, init func(*TaskFeed), assign func(*TaskFeed, *TaskFile)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[ulid.ID]*TaskFeed)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(taskfile.FieldTaskFeedID)
	}
	query.Where(predicate.TaskFile(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(taskfeed.TaskFilesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TaskFeedID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "task_feed_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *TaskFeedQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TaskFeedQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(taskfeed.Table, taskfeed.Columns, sqlgraph.NewFieldSpec(taskfeed.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, taskfeed.FieldID)
		for i := range fields {
			if fields[i] != taskfeed.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTask != nil {
			_spec.Node.AddColumnOnce(taskfeed.FieldTaskID)
		}
		if _q.withTeammate != nil {
			_spec.Node.AddColumnOnce(taskfeed.FieldTeammateID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TaskFeedQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(taskfeed.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = taskfeed.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedTaskFeedLikes tells the query-builder to eager-load the nodes that are connected to the "taskFeedLikes"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithNamedTaskFeedLikes(name string, opts ...func(*TaskFeedLikeQuery)) *TaskFeedQuery {
	query := (&TaskFeedLikeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedTaskFeedLikes == nil {
		_q.withNamedTaskFeedLikes = make(map[string]*TaskFeedLikeQuery)
	}
	_q.withNamedTaskFeedLikes[name] = query
	return _q
}

// WithNamedTaskFiles tells the query-builder to eager-load the nodes that are connected to the "taskFiles"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *TaskFeedQuery) WithNamedTaskFiles(name string, opts ...func(*TaskFileQuery)) *TaskFeedQuery {
	query := (&TaskFileClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedTaskFiles == nil {
		_q.withNamedTaskFiles = make(map[string]*TaskFileQuery)
	}
	_q.withNamedTaskFiles[name] = query
	return _q
}

// TaskFeedGroupBy is the group-by builder for TaskFeed entities.
type TaskFeedGroupBy struct {
	selector
	build *TaskFeedQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TaskFeedGroupBy) Aggregate(fns ...AggregateFunc) *TaskFeedGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TaskFeedGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TaskFeedQuery, *TaskFeedGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TaskFeedGroupBy) sqlScan(ctx context.Context, root *TaskFeedQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TaskFeedSelect is the builder for selecting fields of TaskFeed entities.
type TaskFeedSelect struct {
	*TaskFeedQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TaskFeedSelect) Aggregate(fns ...AggregateFunc) *TaskFeedSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TaskFeedSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TaskFeedQuery, *TaskFeedSelect](ctx, _s.TaskFeedQuery, _s, _s.inters, v)
}

func (_s *TaskFeedSelect) sqlScan(ctx context.Context, root *TaskFeedQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
