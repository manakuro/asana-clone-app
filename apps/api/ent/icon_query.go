// Code generated by ent, DO NOT EDIT.

package ent

import (
	"asana-clone-app/ent/icon"
	"asana-clone-app/ent/predicate"
	"asana-clone-app/ent/projecticon"
	"asana-clone-app/ent/schema/ulid"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// IconQuery is the builder for querying Icon entities.
type IconQuery struct {
	config
	ctx                   *QueryContext
	order                 []icon.OrderOption
	inters                []Interceptor
	predicates            []predicate.Icon
	withProjectIcons      *ProjectIconQuery
	modifiers             []func(*sql.Selector)
	loadTotal             []func(context.Context, []*Icon) error
	withNamedProjectIcons map[string]*ProjectIconQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IconQuery builder.
func (_q *IconQuery) Where(ps ...predicate.Icon) *IconQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *IconQuery) Limit(limit int) *IconQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *IconQuery) Offset(offset int) *IconQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *IconQuery) Unique(unique bool) *IconQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *IconQuery) Order(o ...icon.OrderOption) *IconQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryProjectIcons chains the current query on the "projectIcons" edge.
func (_q *IconQuery) QueryProjectIcons() *ProjectIconQuery {
	query := (&ProjectIconClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(icon.Table, icon.FieldID, selector),
			sqlgraph.To(projecticon.Table, projecticon.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, icon.ProjectIconsTable, icon.ProjectIconsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Icon entity from the query.
// Returns a *NotFoundError when no Icon was found.
func (_q *IconQuery) First(ctx context.Context) (*Icon, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{icon.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *IconQuery) FirstX(ctx context.Context) *Icon {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Icon ID from the query.
// Returns a *NotFoundError when no Icon ID was found.
func (_q *IconQuery) FirstID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{icon.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *IconQuery) FirstIDX(ctx context.Context) ulid.ID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Icon entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Icon entity is found.
// Returns a *NotFoundError when no Icon entities are found.
func (_q *IconQuery) Only(ctx context.Context) (*Icon, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{icon.Label}
	default:
		return nil, &NotSingularError{icon.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *IconQuery) OnlyX(ctx context.Context) *Icon {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Icon ID in the query.
// Returns a *NotSingularError when more than one Icon ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *IconQuery) OnlyID(ctx context.Context) (id ulid.ID, err error) {
	var ids []ulid.ID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{icon.Label}
	default:
		err = &NotSingularError{icon.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *IconQuery) OnlyIDX(ctx context.Context) ulid.ID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Icons.
func (_q *IconQuery) All(ctx context.Context) ([]*Icon, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Icon, *IconQuery]()
	return withInterceptors[[]*Icon](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *IconQuery) AllX(ctx context.Context) []*Icon {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Icon IDs.
func (_q *IconQuery) IDs(ctx context.Context) (ids []ulid.ID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(icon.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *IconQuery) IDsX(ctx context.Context) []ulid.ID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *IconQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*IconQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *IconQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *IconQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *IconQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IconQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *IconQuery) Clone() *IconQuery {
	if _q == nil {
		return nil
	}
	return &IconQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]icon.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.Icon{}, _q.predicates...),
		withProjectIcons: _q.withProjectIcons.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithProjectIcons tells the query-builder to eager-load the nodes that are connected to
// the "projectIcons" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *IconQuery) WithProjectIcons(opts ...func(*ProjectIconQuery)) *IconQuery {
	query := (&ProjectIconClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProjectIcons = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Icon.Query().
//		GroupBy(icon.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *IconQuery) GroupBy(field string, fields ...string) *IconGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IconGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = icon.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Icon.Query().
//		Select(icon.FieldName).
//		Scan(ctx, &v)
func (_q *IconQuery) Select(fields ...string) *IconSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &IconSelect{IconQuery: _q}
	sbuild.label = icon.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IconSelect configured with the given aggregations.
func (_q *IconQuery) Aggregate(fns ...AggregateFunc) *IconSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *IconQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !icon.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *IconQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Icon, error) {
	var (
		nodes       = []*Icon{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withProjectIcons != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Icon).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Icon{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withProjectIcons; query != nil {
		if err := _q.loadProjectIcons(ctx, query, nodes,
			func(n *Icon) { n.Edges.ProjectIcons = []*ProjectIcon{} },
			func(n *Icon, e *ProjectIcon) { n.Edges.ProjectIcons = append(n.Edges.ProjectIcons, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedProjectIcons {
		if err := _q.loadProjectIcons(ctx, query, nodes,
			func(n *Icon) { n.appendNamedProjectIcons(name) },
			func(n *Icon, e *ProjectIcon) { n.appendNamedProjectIcons(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *IconQuery) loadProjectIcons(ctx context.Context, query *ProjectIconQuery, nodes []*Icon, init func(*Icon), assign func(*Icon, *ProjectIcon)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[ulid.ID]*Icon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(projecticon.FieldIconID)
	}
	query.Where(predicate.ProjectIcon(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(icon.ProjectIconsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.IconID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "icon_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *IconQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *IconQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(icon.Table, icon.Columns, sqlgraph.NewFieldSpec(icon.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, icon.FieldID)
		for i := range fields {
			if fields[i] != icon.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *IconQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(icon.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = icon.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedProjectIcons tells the query-builder to eager-load the nodes that are connected to the "projectIcons"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *IconQuery) WithNamedProjectIcons(name string, opts ...func(*ProjectIconQuery)) *IconQuery {
	query := (&ProjectIconClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedProjectIcons == nil {
		_q.withNamedProjectIcons = make(map[string]*ProjectIconQuery)
	}
	_q.withNamedProjectIcons[name] = query
	return _q
}

// IconGroupBy is the group-by builder for Icon entities.
type IconGroupBy struct {
	selector
	build *IconQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *IconGroupBy) Aggregate(fns ...AggregateFunc) *IconGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *IconGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IconQuery, *IconGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *IconGroupBy) sqlScan(ctx context.Context, root *IconQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IconSelect is the builder for selecting fields of Icon entities.
type IconSelect struct {
	*IconQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *IconSelect) Aggregate(fns ...AggregateFunc) *IconSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *IconSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IconQuery, *IconSelect](ctx, _s.IconQuery, _s, _s.inters, v)
}

func (_s *IconSelect) sqlScan(ctx context.Context, root *IconQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
